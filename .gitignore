node_modules/
dist/
dist-prod/
*.zip
.DS_Store
# SPEC.md is a private doc (symlinked from the shortlisted-cloud repo).
# CLAUDE.md / AGENTS.md ARE committed here — technical conventions, no business content.
SPEC.md

# Env & secrets (none used today; standard guard)
.env
.env.*
!.env.example
*.log
